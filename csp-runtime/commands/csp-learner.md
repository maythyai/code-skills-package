---
description: "Extract a reusable skill from the current session workflow, a deprecated alias for the skillify command"
---

# CSP learner

`/code-skills-package:learner` is a compatibility alias for `/code-skills-package:skillify`.

Read `skills/skillify/SKILL.md`, follow its full instructions, and pass through the user's arguments:

```text
$ARGUMENTS
```
