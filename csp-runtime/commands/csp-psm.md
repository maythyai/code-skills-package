---
description: "Compatibility alias for project-session-manager, managing worktree-based dev environments with tmux sessions"
---

# CSP psm

`/code-skills-package:psm` is a compatibility alias for `/code-skills-package:project-session-manager`.

Read `skills/project-session-manager/SKILL.md`, follow its full instructions, and pass through the user's arguments:

```text
$ARGUMENTS
```
